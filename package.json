{
  "name": "good-intentions",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0 <23"
  },
  "scripts": {
    "generate:checkout": "node scripts/generate-checkout-catalog.mjs",
    "predev": "npm run generate:checkout",
    "dev": "astro dev",
    "prebuild": "npm run generate:checkout",
    "build": "astro build",
    "preview": "astro preview",
    "precheck": "npm run generate:checkout",
    "check": "astro check && tsc --noEmit",
    "verify:content": "node scripts/verify-content-config.mjs",
    "verify:launch": "node scripts/verify-content-config.mjs --launch"
  },
  "dependencies": {
    "astro": "7.0.9"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.9",
    "typescript": "5.9.3"
  }
}
